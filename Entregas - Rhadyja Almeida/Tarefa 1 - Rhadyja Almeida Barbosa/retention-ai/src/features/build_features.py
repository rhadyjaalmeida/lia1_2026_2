"""
Funções de engenharia de atributos (feature engineering) para o projeto
RetentionAI.

Este módulo é um ponto de extensão: mantenha no notebook
(`notebooks/01_churn_analysis.ipynb`) o desenvolvimento exploratório das
features primeiro. Somente após validar que uma transformação funciona
corretamente e será reutilizada, migre-a para uma função aqui.

Exemplo de padrão a seguir (adicionar conforme o projeto evoluir):

    def add_recency_features(df: pd.DataFrame) -> pd.DataFrame:
        ...
        return df
"""
